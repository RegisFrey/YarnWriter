/*
  multi-window will only apply in electron,
  so won't be managed in vuex rather managed at a higher level.

  A window *can* contain multiple documents (e.g. as tabs) so that
  concept is moving to the more aptly named "documents".
*/
